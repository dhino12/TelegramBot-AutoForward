const { bot } = require("../../server")

// const sendMessageHandler = async (chat_id, text, other) => {
//     return  await bot.api.sendMessage(chat_id, text, other)
// }

// const forwardMessageHandler = async (chat_id, from_chat_id, id_chat) => {
//     return await bot.api.forwardMessage(chat_id, from_chat_id, id_chat)
// }

// module.exports = { sendMessageHandler, forwardMessageHandler }